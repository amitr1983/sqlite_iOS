# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'sql_storage_exercise' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for sql_storage_exercise
  pod 'SQLite.swift'

  target 'sql_storage_exerciseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'sql_storage_exerciseUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
